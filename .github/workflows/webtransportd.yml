# Cycle 36: CI across linux-gcc, macos-clang, and windows-mingw.
#
# All three jobs install only the stock toolchain — no external TLS
# package — because mbedtls + picoquic + picotls are all vendored
# under thirdparty/. On Linux and macOS `make test` runs every
# *_test.c under ASAN + UBSAN (our Makefile contract), then a
# --version smoke on the daemon binary, then upload.
#
# Windows runs via MSYS2 + mingw-w64. Sanitizers aren't reliable on
# that toolchain and some POSIX bits child_process.c depends on
# (fork, execvp) are emulated rather than native, so the Windows
# job disables sanitizers via CFLAGS/LDFLAGS overrides. The native
# Win32 `child_process_win.c` from the Wanted list would replace
# that emulation with a CreateProcess-based implementation.

name: webtransportd

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  posix:
    strategy:
      fail-fast: false
      matrix:
        include:
          - { name: linux-gcc,   os: ubuntu-latest, cc: gcc }
          - { name: macos-clang, os: macos-latest,  cc: clang }

    name: ${{ matrix.name }}
    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Show toolchain
        run: |
          ${{ matrix.cc }} --version
          make --version

      # make test: compiles every module + vendored TU, runs every
      # test binary under -fsanitize=address,undefined with -Werror
      # on our first-party code. Vendored TUs keep -w so upstream
      # warnings don't fight the build.
      - name: make test
        run: make test CC=${{ matrix.cc }}

      # The test build above links ASAN + UBSAN, which makes the
      # resulting ./webtransportd depend on libclang_rt.asan at
      # runtime. That dylib isn't installed on end-user machines —
      # an operator who downloads the artifact just sees a cryptic
      # dyld "Library not loaded: libclang_rt.asan_osx_dynamic.dylib"
      # abort. Rebuild the daemon without sanitizers for the
      # artifact upload so it runs standalone.
      - name: Rebuild release binary (no sanitizers)
        env:
          CFLAGS: "-O2 -Wall -Wextra -Werror -std=c11 -D_GNU_SOURCE -D_DARWIN_C_SOURCE -fno-omit-frame-pointer -pthread"
          LDFLAGS: "-pthread"
          NO_SANITIZER: "1"
        run: |
          make clean
          make webtransportd CC=${{ matrix.cc }}

      # Daemon startup smoke on the release build.
      - name: daemon --version smoke
        run: ./webtransportd --version

      - name: Upload webtransportd binary
        uses: actions/upload-artifact@v4
        with:
          name: webtransportd-${{ matrix.name }}
          path: webtransportd
          if-no-files-found: error
          retention-days: 14

  windows:
    name: windows-mingw
    runs-on: windows-latest
    defaults:
      run:
        shell: msys2 {0}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install MSYS2 + mingw-w64 toolchain
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          update: true
          install: >-
            mingw-w64-x86_64-gcc
            mingw-w64-x86_64-make
            make

      - name: Show toolchain
        run: |
          gcc --version
          make --version

      # Sanitizers are unreliable on mingw-w64; disable them via the
      # same CFLAGS/LDFLAGS knobs our Makefile honors (?= defaults).
      # -pthread / -std=c11 / -D_GNU_SOURCE / -D_DARWIN_C_SOURCE need
      # to match the Makefile's default CFLAGS so feature-test macros
      # stay consistent with linux-gcc and macos-clang (glibc hides
      # strdup/setenv/unsetenv/environ without _GNU_SOURCE; Darwin
      # keeps BSD extensions visible when _POSIX_C_SOURCE-style gating
      # is active, no-op on mingw).
      - name: make test (no sanitizer)
        env:
          CFLAGS: "-O0 -g -Wall -Wextra -Werror -std=c11 -D_GNU_SOURCE -D_DARWIN_C_SOURCE -fno-omit-frame-pointer -pthread"
          LDFLAGS: "-pthread"
        run: make test

      - name: daemon --version smoke
        run: ./webtransportd.exe --version

      - name: Upload webtransportd binary
        uses: actions/upload-artifact@v4
        with:
          name: webtransportd-windows-mingw
          path: webtransportd.exe
          if-no-files-found: error
          retention-days: 14
